[project]
name = "mathlib-stats"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.14"
dependencies = [
    "datasets>=4.4.1",
    "jinja2>=3.1.6",
    "pyarrow>=22.0.0",
]
